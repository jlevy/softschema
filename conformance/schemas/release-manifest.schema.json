{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:softschema:draft:conformance:release-manifest:v0",
  "title": "external softschema release manifest",
  "type": "object",
  "required": ["schema_version", "logical_version", "source_commit", "subjects"],
  "properties": {
    "schema_version": { "const": "1" },
    "logical_version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+(?:-rc\\.[0-9]+)?$"
    },
    "source_commit": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "subjects": {
      "type": "object",
      "description": "The release driver rejects a manifest when the sum of all declared subject sizes exceeds 1073741824 bytes (1 GiB). JSON Schema validates each subject; this cross-subject sum is a semantic invariant.",
      "minProperties": 1,
      "propertyNames": {
        "type": "string",
        "pattern": "^[A-Za-z0-9][A-Za-z0-9._+-]*$"
      },
      "additionalProperties": { "$ref": "#/$defs/subject" }
    }
  },
  "$defs": {
    "subject": {
      "type": "object",
      "required": ["kind", "media_type", "size", "sha256"],
      "properties": {
        "kind": {
          "enum": [
            "wheel",
            "sdist",
            "npm",
            "conformance",
            "release_metadata",
            "build_metadata",
            "sbom"
          ]
        },
        "media_type": { "type": "string", "minLength": 1 },
        "size": { "type": "integer", "minimum": 1, "maximum": 536870912 },
        "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
